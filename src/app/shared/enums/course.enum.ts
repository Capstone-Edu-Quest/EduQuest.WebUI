export enum MissionStatus {
    DONE = 'Done',
    CURRENT = 'Current',
    LOCKED = 'Locked'
}

export enum CourseSortEnum {
    SORT_MOST_FEEDBACKS = 1,
    SORT_NEWEST,
    SORT_HIGHEST_RATING
}

export enum MaterialTypeEnum {
    VIDEO = 1,
    DOCUMENT,
    QUIZ,
    ASSIGNMENT
}

export enum InstructorCourseStatus {
    DRAFT = "Draft",
    PENDING = "Pending",
    PUBLIC = "Public"
}